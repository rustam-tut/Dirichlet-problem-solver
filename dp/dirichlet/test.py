import method as m
import numpy as np
from myf.myfunc import myfin

if __name__ == "__main__":
    # f = lambda x, y: -(-np.pi ** 2 * np.sin(np.pi * x / 3) * np.sin(np.pi * y / 3))
    # u = lambda x, y: (9 / 2) * np.sin(np.pi * x / 3) * np.sin(np.pi * y / 3)
    # g = lambda x, y: (9 / 2) * np.sin(np.pi * x / 3) * np.sin(np.pi * y / 3)
    
    # # f = lambda x, y: -(6 - 16)
    # # u = lambda x, y: 3 * x ** 2 - 8 * y ** 2 + 13
    # # g = lambda x, y: 3 * x ** 2 - 8 * y ** 2 + 13

    # a1 = 0
    # a2 = 3
    # b1 = 0
    # b2 = 3
    # Nx = 3
    # Ny = 3

    # ds = m.JacobiSolver(u, f, g, a1, a2, b1, b2, Nx=Nx, Ny=Ny)
    # ds.solve(10e-3)
